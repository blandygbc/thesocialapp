import 'package:flutter/material.dart';

class StoryDetailsView extends StatelessWidget {
  static String routeName = "/story-details";
  const StoryDetailsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
